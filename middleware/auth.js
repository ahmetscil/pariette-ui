export default function ({ store, error }) {
  alert('store.state.pariette')
  // if (!store.state.authUser) {
  //   error({
  //     message: 'You are not connected',
  //     statusCode: 403
  //   })
  // }
}
